/*
  this is a component for the site that renders posts
  NavBar always stays on, only post view changes 
  elements are located in components
  there are two view options: grid of posts or one big post
*/ 

export default function Content() {
  return(
    <div class='w-full h-full'>
      
    </div>
  )
}